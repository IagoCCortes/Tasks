import React from 'react';
import {StyleSheet, Text} from 'react-native';

const Profile = () => <Text style={styles.header}>Profile</Text>;

const styles = StyleSheet.create({
  header: {
    fontSize: 80,
  },
});

export default Profile;
